name: Swift
on:
  push:
    branches: [ iOS ]
  pull_request:
    branches: [ master, iOS ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v3
    - run: cd iOS
    - name: Build
      run: xcodebuild clean build
    - name: Run tests
      run: xcodebuild clean test
